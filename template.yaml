AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  economins-sam

  Sample SAM Template for economins-sam

Parameters:
  RebApiKey:
    Type: String
    NoEcho: true
  EcosApiKey:
    Type: String
    NoEcho: true
  KrxApiKey:
    Type: String
    NoEcho: true
  OpenAiKey:
    Type: String
    NoEcho: true
  DataGoKrKey:
    Type: String
    NoEcho: true
  DdbTable:
    Type: String
    NoEcho: true
  SlackWebhookUrl:
    Type: String
    NoEcho: true
  LambdaRoleName:
    Type: String
  AccountId:
    Type: String
# More info about Globals: https://github.com/awslabs/serverless-application-model/blob/master/docs/globals.rst
Globals:
  Function:
    Runtime: python3.13
    Timeout: 10
    Environment:
      Variables:
        REB_API_KEY: !Ref RebApiKey
        ECOS_API_KEY: !Ref EcosApiKey
        KRX_API_KEY: !Ref KrxApiKey
        OPENAI_API_KEY: !Ref OpenAiKey
        DATA_GO_KR_API_KEY: !Ref DataGoKrKey
        DDB_TABLE: !Ref DdbTable
        SLACK_WEBHOOK_URL: !Ref SlackWebhookUrl
        LambdaRoleName: !Ref LambdaRoleName
        AccountId: !Ref AccountId
        S3_BUCKET_NAME: economins
    # You can add LoggingConfig parameters such as the Logformat, Log Group, and SystemLogLevel or ApplicationLogLevel. Learn more here https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-resource-function.html#sam-function-loggingconfig.
    LoggingConfig:
      LogFormat: JSON

Resources:
  CommonLayer:
    Type: AWS::Serverless::LayerVersion
    Properties:
      LayerName: economins-common
      ContentUri: layers/common
      CompatibleRuntimes:
        - python3.13

  CollectAptPriceAll:
    Type: AWS::Serverless::Function 
    Properties:
      FunctionName: collect-reb-apt-price-index-all
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: reb/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          CLS_ID: '500001'
          S3_OUTPUT_KEY: data/apt-price-index-all.json
          STATBL_ID: A_2024_00178
      Events:
        CollectAptPriceAllSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectAptPriceGreaterSeoul:
    Type: AWS::Serverless::Function 
    Properties:
      FunctionName: collect-reb-apt-price-index-greater-seoul
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: reb/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          CLS_ID: '500002'
          S3_OUTPUT_KEY: data/apt-price-index-greater-seoul.json
          STATBL_ID: A_2024_00178
      Events:
        CollectAptPriceGreaterSeoulSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectAptPriceSeoul:
    Type: AWS::Serverless::Function 
    Properties:
      FunctionName: collect-reb-apt-price-index-seoul
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: reb/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          CLS_ID: '500007'
          S3_OUTPUT_KEY: data/apt-price-index-seoul.json
          STATBL_ID: A_2024_00178
      Events:
        CollectAptPriceSeoulSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectAptVolumeAll:
    Type: AWS::Serverless::Function 
    Properties:
      FunctionName: collect-reb-apt-volume-all
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: reb/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          CLS_ID: '500001'
          S3_OUTPUT_KEY: data/apt-volume-all.json
          STATBL_ID: A_2024_00596
          GRP_ID: '900001'
          ITM_ID: '100001'
      Events:
        CollectAptVolumeAllSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectAptVolumeGreaterSeoul:
    Type: AWS::Serverless::Function 
    Properties:
      FunctionName: collect-reb-apt-volume-greater-seoul
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: reb/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          CLS_ID: '500001'
          S3_OUTPUT_KEY: data/apt-volume-kyeongki.json
          STATBL_ID: A_2024_00596
          GRP_ID: '900010'
          ITM_ID: '100001'
      Events:
        CollectAptVolumeGreaterSeoulSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 21 2,10 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectAptVolumeSeoul:
    Type: AWS::Serverless::Function 
    Properties:
      FunctionName: collect-reb-apt-volume-seoul
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: reb/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          CLS_ID: '500001'
          S3_OUTPUT_KEY: data/apt-volume-seoul.json
          STATBL_ID: A_2024_00596
          GRP_ID: '900002'
          ITM_ID: '100001'
      Events:
        CollectAptVolumeSeoulSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectBaseRateKorea:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-base-rate-korea
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/base-rate-korea.json
          STAT_CODE: '722Y001'
          CYCLE: 'M'
          ITEM_CODE: '0101000'
      Events:
        CollectBaseRateKoreaSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectCorporateBondRateKorea3AA:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-corporate-bond-korea-3-aa
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/corporate-bond-korea-3-aa.json
          STAT_CODE: '721Y001'
          CYCLE: 'M'
          ITEM_CODE: '7020000'
      Events:
        CollectCorporateBondRateKorea3AASchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectTreasuryBondKorea3:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-treasury-bond-korea-3
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/treasury-bond-korea-3.json
          STAT_CODE: '721Y001'
          CYCLE: 'M'
          ITEM_CODE: '5020000'
      Events:
        CollectTreasuryBondKorea3Schedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectTreasuryBondKorea10:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-treasury-bond-korea-10
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/treasury-bond-korea-10.json
          STAT_CODE: '721Y001'
          CYCLE: 'M'
          ITEM_CODE: '5050000'
      Events:
        CollectTreasuryBondKorea10Schedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectMortgageLoanRateKorea:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-mortgage-load-rate-korea
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/mortgage-rate-korea.json
          STAT_CODE: '121Y006'
          CYCLE: 'M'
          ITEM_CODE: 'BECBLA0302'
      Events:
        CollectMortgageLoanRateKoreaSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectHouseholdCreditsKorea:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-household-credits-korea
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/household-credits-korea.json
          STAT_CODE: '151Y001'
          CYCLE: 'Q'
          ITEM_CODE: '1100000'
      Events:
        CollectHouseholdCreditsKoreaSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectCurrenctAccountBalanceKorea:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-current-account-balance-korea
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/current-account-balance-korea.json
          STAT_CODE: '301Y017'
          CYCLE: 'M'
          ITEM_CODE: 'SA000'
      Events:
        CollectCurrenctAccountBalanceKoreaSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectExchangeRateDollarKorea:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-exchange-rate-dollar-korea
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/exchange-rate-dollar-korea.json
          STAT_CODE: '731Y004'
          CYCLE: 'M'
          ITEM_CODE: '0000001'
          ITEM_CODE2: '0000200'
      Events:
        CollectExchangeRateDollarKoreaSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectM2Korea:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-m2-korea
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/m2-korea.json
          STAT_CODE: '161Y006'
          CYCLE: 'M'
          ITEM_CODE: 'BBHA00'
      Events:
        CollectM2KoreaSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectCPIKorea:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-cpi-korea
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/cpi-korea.json
          STAT_CODE: '901Y009'
          CYCLE: 'M'
          ITEM_CODE: '0'
      Events:
        CollectCPIKoreaSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectPPIKorea:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-ppi-korea
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/ppi-korea.json
          STAT_CODE: '404Y014'
          CYCLE: 'M'
          ITEM_CODE: '*AA'
      Events:
        CollectPPIKoreaSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectRealGDPKorea:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-real-gdp-korea
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/real-gdp-korea.json
          STAT_CODE: '200Y104'
          CYCLE: 'Q'
          ITEM_CODE: '1400'
      Events:
        CollectRealGDPKoreaSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectUnemploymentRateKorea:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-unemployment-rate-korea
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/unemployment-rate-korea.json
          STAT_CODE: '901Y027'
          CYCLE: 'M'
          ITEM_CODE: 'I61BC'
          ITEM_CODE2: 'I28B'
      Events:
        CollectUnemploymentRateKoreaSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectForeignExchangeReservesKorea:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-ecos-foreign-exchange-reserves-korea
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: ecos/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/foreign-exchange-reserves-korea.json
          STAT_CODE: '732Y001'
          CYCLE: 'M'
          ITEM_CODE: '99'
      Events:
        CollectForeignExchangeReservesKoreaSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 2,9,16,23 * ? *)
            Description: Trigger Lambda on 2,9,16,23 every month at 6PM KST

  CollectKospi:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-krx-kospi
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: krx/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/kospi.json
          INDEX_TYPE: kospi_dd_trd
      Events:
        CollectKospiSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 1,2,3 * ? *)
            Description: Trigger Lambda on 1,2,3 every month at 6PM KST

  CollectKosdaq:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-krx-kosdaq
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: krx/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/kosdaq.json
          INDEX_TYPE: kosdaq_dd_trd
      Events:
        CollectKosdaqSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 1,2,3 * ? *)
            Description: Trigger Lambda on 1,2,3 every month at 6PM KST

  CollectWti:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-yahoo-wti
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: yahoo/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/oil.json
          YAHOO_SYMBOL: CL=F
      Events:
        CollectWtiSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 3 * ? *)
            Description: Trigger Lambda on 3 every month at 6PM KST

  CollectGold:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-yahoo-gold
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: yahoo/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_OUTPUT_KEY: data/gold.json
          YAHOO_SYMBOL: GC=F
      Events:
        CollectGoldSchedule:
          Type: Schedule
          Properties:
            Schedule: cron(0 9 3 * ? *)
            Description: Trigger Lambda on 3 every month at 6PM KST

  CollectMonetaryPolicyIssue:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-bok-monetary-policy-issue
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: bok/
      Handler: app_issue.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          BOK_PAGE_URL: https://www.bok.or.kr/portal/singl/crncyPolicyDrcMtg/listYear.do?mtgSe=A&menuNo=200755
      Timeout: 360
      Events:
        CollectMonetaryPolicyIssueSchedule:
          Type: Schedule
          Properties:
            Schedule: "cron(0 5 ? * THU,FRI *)"
            Description: "매주 목,금요일 KST 14:00에 실행"
  
  CollectMonetaryPolicyDecision:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-bok-monetary-policy-decision
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: bok/
      Handler: app_decision.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          BOK_PAGE_URL: https://www.bok.or.kr/portal/singl/crncyPolicyDrcMtg/listYear.do?mtgSe=A&menuNo=200755
      Timeout: 180
      Events:
        CollectMonetaryPolicyDecisionSchedule:
          Type: Schedule
          Properties:
            Schedule: "cron(0 6 ? * THU,FRI *)"
            Description: "매주 목,금요일 KST 15:00에 실행"

  CollectMonetaryPolicyBatchOutput:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-bok-monetary-policy-batch-output
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: bok/
      Handler: app_batch.lambda_handler
      Layers:
        - !Ref CommonLayer
      Timeout: 180
      Events:
        CollectMonetaryPolicyBatchOutputSchedule:
          Type: Schedule
          Properties:
            Schedule: "cron(0 7 ? * THU,FRI *)"
            Description: "매주 목,금요일 KST 16:00에 실행"
  
  CollectMolitAptTransaction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-molit-apt-transaction
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: molit/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_BUCKET_NAME: economins-raw
          TRADE_TYPE: SELL
          PUBLIC_API_URL: https://apis.data.go.kr/1613000/RTMSDataSvcAptTradeDev/getRTMSDataSvcAptTradeDev
      Timeout: 600
      Events:
        CollectMolitAptTransactionCurrentSchedule:
          Type: Schedule
          Properties:
            Schedule: "cron(0 8 3 * ? *)"
            Description: "매월 3일 KST 17:00에 실행"
            Input: '{"month_offset": 0}'
        CollectMolitAptTransactionPrevSchedule:
          Type: Schedule
          Properties:
            Schedule: "cron(20 8 3 * ? *)"
            Description: "매월 3일 KST 17:20에 실행"
            Input: '{"month_offset": 1}'
        CollectMolitAptTransactionPrev2Schedule:
          Type: Schedule
          Properties:
            Schedule: "cron(40 8 3 * ? *)"
            Description: "매월 3일 KST 17:40에 실행"
            Input: '{"month_offset": 2}'

  CollectMolitAptRent:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: collect-molit-apt-rent
      Role: !Sub arn:aws:iam::${AccountId}:role/${LambdaRoleName}
      CodeUri: molit/
      Handler: app.lambda_handler
      Layers:
        - !Ref CommonLayer
      Environment:
        Variables:
          S3_BUCKET_NAME: economins-raw
          TRADE_TYPE: RENT
          PUBLIC_API_URL: https://apis.data.go.kr/1613000/RTMSDataSvcAptRent/getRTMSDataSvcAptRent
      Timeout: 600
      Events:
        CollectMolitAptRentCurrentSchedule:
          Type: Schedule
          Properties:
            Schedule: "cron(0 9 3 * ? *)"
            Description: "매월 3일 KST 18:00에 실행"
            Input: '{"month_offset": 0}'
        CollectMolitAptRentPrevSchedule:
          Type: Schedule
          Properties:
            Schedule: "cron(20 9 3 * ? *)"
            Description: "매월 3일 KST 18:20에 실행"
            Input: '{"month_offset": 1}'
        CollectMolitAptRentPrev2Schedule:
          Type: Schedule
          Properties:
            Schedule: "cron(40 9 3 * ? *)"
            Description: "매월 3일 KST 18:40에 실행"
            Input: '{"month_offset": 2}'